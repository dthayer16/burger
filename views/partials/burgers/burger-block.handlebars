<li class="list-group-item">
	<span>{{name}}</span>

	<button class="change-devoured btn btn-success float-right" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}DEVOURE{{else}}ANOTHER ONE!{{/if}}
	</button>

	<button class="mr-2 delete-burger btn btn-danger float-right" data-id="{{this.id}}">DELETE!</button>


</li>